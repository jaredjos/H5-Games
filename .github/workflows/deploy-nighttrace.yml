name: Deploy Nighttrace

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - games/nighttrace/**
      - .github/workflows/deploy-nighttrace.yml

permissions:
  contents: write

concurrency:
  group: deploy-nighttrace
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: games/nighttrace/pnpm-lock.yaml

      - name: Install locked dependencies
        working-directory: games/nighttrace
        run: pnpm install --frozen-lockfile

      - name: Build the production game
        working-directory: games/nighttrace
        run: pnpm build

      - name: Publish Nighttrace to GitHub Pages
        shell: bash
        run: |
          GAME_VERSION="$(node -p "require('./games/nighttrace/package.json').version")"
          git worktree add --detach /tmp/h5-pages origin/gh-pages
          rm -rf /tmp/h5-pages/nighttrace
          mkdir -p /tmp/h5-pages/nighttrace
          cp -a games/nighttrace/dist/. /tmp/h5-pages/nighttrace/
          touch /tmp/h5-pages/.nojekyll

          cd /tmp/h5-pages
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A nighttrace .nojekyll

          if git diff --cached --quiet; then
            echo "Nighttrace Pages build is already current."
            exit 0
          fi

          git commit -m "deploy(nighttrace): publish v${GAME_VERSION}"
          git push origin HEAD:gh-pages
